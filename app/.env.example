# Copiar a .env.local (gitignorado) y completar con los valores impresos por
# `npx supabase start` (instancia local) o desde Project Settings > API en supabase.com (producción).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
